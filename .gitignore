# 執行期產生的使用者資料(可在 sidebar 按「📤 雲端備份學習進度」推回 repo,
# 重新部署後自動恢復;但本機 commit 預設不要包,避免 git status 雜訊)
dashboard_data.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.streamlit/secrets.toml

# 防禦性備份(代碼淨化任務暫存)
*.bak
